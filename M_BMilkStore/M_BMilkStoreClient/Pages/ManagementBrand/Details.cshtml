@page
@model M_BMilkStoreClient.Pages.ManagementBrand.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>ProductBrand</h4>
    <hr />
    @if (!string.IsNullOrEmpty(Model.MessageError))
    {
        <div class="alert alert-danger" role="alert">
            @Model.MessageError
        </div>
    }
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ProductBrand.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ProductBrand.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.ProductBrand.ProductBrandId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
